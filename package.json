{
  "dependencies": {
    "node": "^21.1.0"
  },
  "scripts": {
    "heroku-prebuild": "cd client && npm install",
    "start": "cd client && npm start",
    "heroku-postbuild": "cd .. && cd server && npm install"
  }
}
