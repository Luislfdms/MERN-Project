{
  "dependencies": {
    "node": "^21.1.0"
  },
  "scripts": {
    "start": "server/server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install npm && cd client && npm run build"
  }
}


