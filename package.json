{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "express": "^4.17.1",
    "body-parser": "^1.19.0",
    "mongoose": "^6.1.4"
  },
  "scripts": {
    "heroku-prebuild": "cd client && npm install",
    "start": "cd client && npm start",
    "heroku-postbuild": "cd server && npm install && node server.js"
  }
}

